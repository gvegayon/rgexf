% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.gexf}
\alias{plot.gexf}
\title{Visualizing GEXF graphs using sigma.js (default) or gexf-js}
\usage{
\method{plot}{gexf}(x, y = NULL, width = NULL, height = NULL, ...)
}
\arguments{
\item{x}{An object of class \code{gexf}.}

\item{y}{Ignored.}

\item{width, height}{Widget dimensions in pixels (or \code{NULL} for automatic).}

\item{...}{Ignored (kept for S3 compatibility).}
}
\description{
\code{plot.gexf} renders a \code{gexf} object as an interactive sigma.js htmlwidget.
For the legacy gexf-js file-server approach, use \link{plot_gexfjs}.
}
\details{
\code{plot.gexf} delegates to \link{sigmajs}, which produces an htmlwidget powered by
\href{https://www.sigmajs.org/}{sigma.js}.  Node positions, colours, and sizes
are read directly from the GEXF \verb{viz:*} attributes, so the graph must
contain them.  If they are absent you can round-trip through igraph:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{x <- igraph.to.gexf(gexf.to.igraph(x))
plot(x)
}\if{html}{\out{</div>}}
}
\examples{
if (interactive()) {
  path <- system.file("gexf-graphs/lesmiserables.gexf", package = "rgexf")
  graph <- read.gexf(path)
  plot(graph)
}

}
\seealso{
\code{\link[=sigmajs]{sigmajs()}}, \code{\link[=plot_gexfjs]{plot_gexfjs()}}
}
