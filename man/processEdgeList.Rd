\name{processEdgeList}
\alias{processEdgeList}
\title{Decompose an edge list}
\description{Generates two data frames (nodes and edges) from a list of edges}
\usage{
processEdgeList(x)
}
\arguments{
  \item{x}{A matrix or data frame structured as a list of edges}
}
\details{
  \code{processEdgeList} transforms the input into a two-elements list containing a dataframe of nodes (with columns 'id' and 'label') and a dataframe of edges. The last one is numeric (with columns 'source' and 'target') and based on autogenerated nodes' ids. 
}
\value{
  A list containing two data frames.
}
\author{George Vega Yon \email{george.vega@nodoschile.org}, Jorge Fabrega \email{jorge.fabrega@nodoschile.org}}
\examples{
  edgelist <- matrix(
    c("matthew","john",
      "max","stephen",
      "matthew","stephen"),
    byrow=TRUE, ncol=2)
  
  processEdgeList(edgelist)
}